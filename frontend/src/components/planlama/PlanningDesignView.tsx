import { PlanningTimelineView } from './shared/PlanningTimelineView'

/** Üretim planlama — ince sarmalayıcı (paylaşılan timeline kabuğu). */
export function PlanningDesignView() {
  return <PlanningTimelineView variant="legacy-design" />
}
