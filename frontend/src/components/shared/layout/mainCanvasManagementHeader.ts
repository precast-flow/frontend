/** MainCanvas üst başlık + breadcrumb — yönetim modülleri (Proje, Görev, Müşteri, Kalite, …). */
export const mainCanvasManagementHeaderWrapperClass =
  'mb-2 shrink-0 pt-1 pb-2 md:pt-1.5 ps-[0.6875rem] pe-[0.6875rem]'

export const mainCanvasManagementTitleClass =
  'text-xl font-semibold tracking-tight text-gray-900 dark:text-gray-50 md:text-2xl'

/** Başlık ile breadcrumb arası — yalnızca MainCanvas’ta kullanılır. */
export const mainCanvasManagementBreadcrumbGapClass = 'mt-1.5'
